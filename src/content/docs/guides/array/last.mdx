---
title: last
group: 'Array'
description: Get the last item from a list
---

## 基础用法

给定一个项目数组，如果数组中没有项目，则返回最后一个项目或默认值。

```ts
import { last } from 'radash'

const fish = ['marlin', 'bass', 'trout']

const lastFish = last(fish) // => 'trout'
const lastItem = last([], 'bass') // => 'bass'
```
