---
title: sift
group: 'Array'
description: Remove all falsy items from list
---

## 基础用法

给定一个项目列表，返回一个新列表，其中包含所有非假值（truthy）的项目。

```ts
import { sift } from 'radash'

const fish = ['salmon', null, false, NaN, 'sockeye', 'bass']

sift(fish) // => ['salmon', 'sockeye', 'bass']
```
