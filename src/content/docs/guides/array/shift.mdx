---
title: shift
description: Shift array items by n steps
group: 'Array'
---

## 基础用法

给定一个项目列表，返回一个向右移动n个位置的新数组。

```ts
import { shift } from 'radash'
const arr = [1, 2, 3, 4, 5, 6, 7, 8, 9]
shift(arr, 3) // => [7, 8, 9, 1, 2, 3, 4, 5, 6]
```
